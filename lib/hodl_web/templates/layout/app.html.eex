<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Load required Bootstrap and BootstrapVue CSS -->
<link type="text/css" rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/bootstrap.min.css") %>">
<link type="text/css" rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/bootstrap-vue.min.css") %>">

<!-- Load polyfills to support older browsers -->
<script src="<%= Routes.static_path(@conn, "/js/polyfill.min.js") %>" crossorigin="anonymous"></script>

<!-- Load Vue followed by BootstrapVue -->
<script src="<%= Routes.static_path(@conn, "/js/vue.min.js") %>"></script>
<script src="<%= Routes.static_path(@conn, "/js/bootstrap-vue.min.js") %>"></script>

<!-- Load the following for BootstrapVueIcons support -->
<script src="<%= Routes.static_path(@conn, "/js/bootstrap-vue-icons.min.js") %>"></script>


    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Hodl · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header>
  <script>

    /* Toggle between adding and removing the "responsive" class to topnav when the user clicks on the icon */
function myFunction() {
  var x = document.getElementById("myTopnav");
  if (x.className === "topnav") {
    x.className += " responsive";
  } else {
    x.className = "topnav";
  }
}
    </script>
      <section class="">
        <nav role="navigation">
          <div class="topnav" id="myTopnav">


   <a href="/why-hodl"> Why hodl? </a>
  <a href="<%= Routes.page_path(@conn, :calculator) %>" class="">Create a hodl schedule</a>
  <a href="<%= Routes.quote_alert_path(@conn, :new) %>"> Set a new price alert </a>
  <a href="/plans"> Plans </a>
  <%= if Pow.Plug.current_user(@conn) do %>
  <% else %>
  <a href="<%= Routes.pow_registration_path(@conn, :new) %>" class="">Sign up</a>
  <a href="<%= Routes.pow_session_path(@conn, :new) %>" class="">Login</a>
  <% end %>

  <%= if Pow.Plug.current_user(@conn) do %>

  <span><%= link "Sign out", to: Routes.user_path(@conn, :sign_out), method: :delete %></span>
  <% end %>
  <a href="javascript:void(0);" class="icon" onclick="myFunction()">
    <i class="fa fa-bars"></i>
  </a>
</div>
        </nav>
        <a href="https://howtohodl.org/" class="">
          <img src="<%= Routes.static_path(@conn, "/images/hodl.png") %>" alt="How to hodl Logo"/>
        </a>
        </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
      <hr>
      <br>
      <br>
      <%= link "Terms and Conditions", to: Routes.page_path(@conn, :terms) %> -  <%= link "Privacy Policy", to: Routes.page_path(@conn, :privacy) %>
      </main>
  </body>
</html>
